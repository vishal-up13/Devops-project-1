apiVersion: v1
kind: Service
metadata:
  name: website-service
spec:
  type: NodePort # Strict Assignment Requirement
  selector:
    app: website
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30008 # Strict Assignment Requirement
